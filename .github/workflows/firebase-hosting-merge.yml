# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci && npm run build
      - run: npx firebase-tools@latest deploy --only hosting --project test-kambista-e1ed0 --token ${{ secrets.FIREBASE_DEPLOY_TOKEN }}
      # - uses: FirebaseExtended/action-hosting-deploy@v0
      # with:
      #   repoToken: ${{ secrets.GITHUB_TOKEN }}
      #   firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_TEST_KAMBISTA_E1ED0 }}
      #   channelId: live
      #   projectId: test-kambista-e1ed0
